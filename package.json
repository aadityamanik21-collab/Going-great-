{
  "name": "zenith-core",
  "version": "1.0.0",
  "devDependencies": {
    "tailwindcss": "^3.4.1"
  },
  "scripts": {
    "build": "tailwindcss -i ./input.css -o ./output.css --minify"
  }
}
